
function HelpRemove() {
    return (
        <div>
            <h4>Removing tasks</h4>
            <p>When finished with a task follow these steps to remove the task from the list.<br/>
            <b>'Warning, this is permanant and cannot be undone!'</b></p>
            <ol>
                <li>First navigate to the 'Tasks' page, you should see the list of tasks.</li>
                <li>Find the task you want to remove from the list.</li>
                <li>Click the button 'Remove Task'.</li>
                <li>It should immediately remove the task from the list.</li>
            </ol>
        </div>
    );
}

export default HelpRemove;